# ──────────────────────────────────────────────────────────────────────────────
# Yield Router — Environment Variables
# ──────────────────────────────────────────────────────────────────────────────
#
# Copy this file to .env and fill in the values.
# NEVER commit the .env file to version control.
#
#   cp .env.example .env
#
# ──────────────────────────────────────────────────────────────────────────────

# ── RPC Configuration ─────────────────────────────────────────────────────────
# Base Sepolia public RPC (free, rate-limited)
BASE_SEPOLIA_RPC=https://sepolia.base.org

# Alchemy Base Sepolia RPC (recommended for deployment — higher rate limits)
# BASE_SEPOLIA_RPC=https://base-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# ── Deployer ──────────────────────────────────────────────────────────────────
# Private key of the deployer account. Must have ~0.2 Base Sepolia ETH.
# Generate with: cast wallet new
DEPLOYER_PRIVATE_KEY=0x_YOUR_DEPLOYER_PRIVATE_KEY

# ── Role Addresses ────────────────────────────────────────────────────────────
# For testnet, these can all be the same EOA for convenience.
# For mainnet, governance = TimelockController, guardian = 2/3 Gnosis Safe.

# Governance: receives GOVERNANCE_ROLE and DEFAULT_ADMIN_ROLE.
# Controls adapter management, parameter updates, upgrades.
GOVERNANCE_ADDRESS=0x_YOUR_GOVERNANCE_ADDRESS

# Guardian: receives GUARDIAN_ROLE.
# Can pause deposits and trigger emergency withdrawals.
GUARDIAN_ADDRESS=0x_YOUR_GUARDIAN_ADDRESS

# Keeper: receives KEEPER_ROLE.
# Bot address that calls harvest(), rebalance(), updateAdapterAPYs().
KEEPER_ADDRESS=0x_YOUR_KEEPER_ADDRESS

# Keeper private key (for running keeper scripts)
KEEPER_PRIVATE_KEY=0x_YOUR_KEEPER_PRIVATE_KEY

# Fee recipient: receives minted yrUSDC fee shares on harvest.
FEE_RECIPIENT=0x_YOUR_FEE_RECIPIENT_ADDRESS

# ── Contract Verification ─────────────────────────────────────────────────────
# Basescan API key for contract verification.
# Get one at: https://basescan.org/myapikey
BASESCAN_API_KEY=YOUR_BASESCAN_API_KEY

# ── Post-Deployment (set after running deploy-testnet) ────────────────────────
# Fill these in after deployment completes.

# Vault proxy address (the yrUSDC token)
VAULT_PROXY=

# Mock USDC address
MOCK_USDC=

# Vault implementation address (for verification)
VAULT_IMPL=

# Adapter addresses (for verification and keeper operations)
AAVE_ADAPTER=
MORPHO_ADAPTER=
AERO_ADAPTER=

# ── Keeper Script Configuration ───────────────────────────────────────────────
# Comma-separated APY values in bps, one per adapter (in getAdapters() order).
# Example for 3 adapters: Aave=4.5%, Morpho=6.2%, Aerodrome=8.5%
ADAPTER_APYS=450,620,850

# ── Optional ──────────────────────────────────────────────────────────────────
# Deposit cap override (default: 1M USDC = 1000000000000)
# DEPOSIT_CAP=1000000000000

# Initial USDC mint to deployer (default: 10M USDC = 10000000000000)
# INITIAL_MINT=10000000000000
